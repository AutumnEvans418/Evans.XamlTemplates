<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Evans.XamlTemplates"
             x:Class="Evans.XamlTemplates.MainPage">
    <ContentPage.Resources>
        <!--<StackLayout x:Key="Test">
            <Label Text="Here are the templates:"></Label>
            <Label Text="{Binding Test}"/>
            <Label Text="{Binding Test1}"></Label>
        </StackLayout>-->
    </ContentPage.Resources>
    <StackLayout>
        <local:LabelEntry Label="{Binding Value}" Text="{Binding Value1}"></local:LabelEntry>

        <local:EntryAndPicker Label="{Binding Value}" Text="{Binding Value1}" selectedItem="{Binding Value}" data="{Binding Data}"/>
    </StackLayout>
    
    <!--<StackLayout>
        <Label Text="{Binding Value}"></Label>
        <Label Text="{Binding Value1}"></Label>
        <ContentView Content="{StaticResource Test}">
            <local:TemplateSystem.TemplateProperties>
                <local:BindingPropertyList>
                    <local:BindingProperty Name="Test" Value="{Binding Value}"/>
                    <local:BindingProperty Name="Test1" Value="{Binding Value2}"/>
                </local:BindingPropertyList>
            </local:TemplateSystem.TemplateProperties>
        </ContentView>
    </StackLayout>-->
    

</ContentPage>
